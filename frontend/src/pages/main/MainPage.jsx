import { AppScreen } from '@widgets/appScreen';
import './style.css';

/**
 * MainPage — главная страница приложения
 * @returns {JSX.Element}
 */
export const MainPage = () => {
  return <AppScreen />;
};
