# Чат на Socket.io. Frontend

- Create React App
- Craco - инструмент, который позволяет настраивать конфигурацию Create React App (CRA) без необходимости "выбрасывать" (eject) CRA
- Zustand
- Socket.io